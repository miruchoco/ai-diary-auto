name: Auto Diary Generator

on:
  schedule:
    # 4時間ごとに実行 (0:00, 4:00, 8:00, 12:00, 16:00, 20:00 UTC)
    - cron: '0 */4 * * *'
  workflow_dispatch: # 手動実行ボタン

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # リポジトリへの書き込み権限を与える

    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r agent/requirements.txt

    - name: Run AI Agent
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: python agent/manual_run.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "AI Diary Bot"
        git config --global user.email "bot@example.com"
        git add data/posts.json
        # 日記が生成されて変更があった場合のみコミットする
        git diff --staged --quiet || (git commit -m "📝 Update diary entry" && git push)
